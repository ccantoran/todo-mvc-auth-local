<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <h1>Todos</h1>
    <ul>
    <% todos.forEach( el => { %>
            <li class='todoItem' data-id='<%=el._id%>'>
                <span class='del'> Delete </span>
            </li>
    <% }) %>    
    </ul>

    <h2><%= user.userName %> has <%= left%> things left to do.</h2>

    <form action="/todos/createTodo" method='POST'>
        <input type="text" placeholder="Enter Todo Item" name='todoItem'>
        <input type="submit">
    </form>


    <!-- Catherines Section -->
    <!-- Just Testing/This is the selection -->
  <!-- <label for="mood">Choose your mood</label>
                        <select id="mood">
                            <option value="happy">Happy</option>
                            <option value="sad">Sad</option>
                            <option value="mad">Mad</option>
                        </select> -->

            <!-- changed the action to go to specific route -->
            
            <form action="/todos/createMood" method='POST'>
                    <!-- here would go the above selection -->
                        <input type="text" placeholder="Mood here" name='moodHere'>
                        <input type="submit">
            </form>

            <h2><%= user.userName %> is <%= userMood.mood %> today</h2>

    <!-- Catherines Section -->


    <a href="/logout">Logout</a>

    <script src="js/main.js"></script>
</body>
</html>